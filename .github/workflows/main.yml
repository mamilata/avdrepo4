name: workflow1
on: 
   push:
        branches:
        - main

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: checkout code
          uses: actions/checkout@v2
        - name: checkout python version
          run: python --version
        
